# ファイル取込_例外設計書

## 目的
ファイル取込のエラーハンドリング方針を整理する。

## エラー方針

| ケース | 対応内容 |
| --- | --- |
| ファイル名不正 | `E4001` コードとメッセージを返却 |
| バリデーション失敗 | エラー行のメッセージリストを構築して返却 |
| 実行時例外 | `E5001` コードとログ出力、`JobStatus` に記録 |
| 既処理ファイル | `StorageService.isAlreadyProcessed()` で判定 |

## 参照元
- FileImport.md
