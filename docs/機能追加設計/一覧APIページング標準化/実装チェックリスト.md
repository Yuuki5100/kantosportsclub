# 一覧APIページング標準化_実装チェックリスト

## 0. ゴール（DoD）
- [ ] 対象4API（`/api/user/list`、`/api/roles/list`、`/api/manual/list`、`/system-transfer/batch-list`）が `pageNumber/pageSize/sortKey/sortDir` で動作する
- [ ] 旧クエリ（`page`、`size`、`sortBy`、`pagesize`、`pageNo`、`sortOrder`）を受け付けず `400(E4001)` を返す
- [ ] 一覧レスポンスが `total + pageNumber + pageSize` を返す
- [ ] FE画面が新クエリのみ送信し、画面挙動（ページング・ソート）が回帰しない

## 1. BEチェックリスト
- [ ] `UserListQuery` を `pageNumber/pageSize/sortKey/sortDir` 仕様へ更新した
- [ ] `RoleListQuery` を `pageNumber/pageSize/sortKey/sortDir` 仕様へ更新した
- [ ] `ManualListQuery` を `pageNumber/pageSize/sortKey/sortDir` 仕様へ更新した
- [ ] `BaseListRequest` を `pageNumber/pageSize/sortKey/sortDir` 仕様へ更新した
- [ ] 各Controllerで旧クエリ拒否チェックを実装した
- [ ] 各Controllerでバリデーションエラー時に `400(E4001)` を返すことを確認した
- [ ] User一覧の `sortKey` ホワイトリストを実装した
- [ ] Role一覧の `sortKey` ホワイトリストを実装した
- [ ] Manual一覧の `sortKey` ホワイトリストを実装した
- [ ] Batch一覧の `sortKey` ホワイトリストを実装した
- [ ] `sortDir` は `asc/desc` 以外を拒否することを実装した
- [ ] User/Role/Manual で `PageRequest.of(pageNumber - 1, pageSize, sort)` を適用した
- [ ] Batch で `pageNumber -> pageNo` の1始まり変換を適用した
- [ ] User一覧レスポンスに `pageNumber/pageSize` を追加した
- [ ] Role一覧レスポンスに `pageNumber/pageSize` を追加した
- [ ] Manual一覧レスポンスに `pageNumber/pageSize` を追加した
- [ ] Batch一覧レスポンスに `pageNumber/pageSize` を追加した
- [ ] BE単体/結合テスト（正常系）を追加した
- [ ] BE単体/結合テスト（異常系: 不正値・旧クエリ）を追加した
- [ ] 4APIの回帰確認（件数、総件数、ソート順）を完了した

## 2. FEチェックリスト
- [ ] 共通クエリ型を `pageNumber/pageSize/sortKey/sortDir` に統一した
- [ ] `userService` の一覧リクエリを新キーへ置換した
- [ ] `roleService` の一覧リクエリを新キーへ置換した
- [ ] `manualService` の一覧リクエリを新キーへ置換した
- [ ] `batchResultService` の一覧リクエリを新キーへ置換した
- [ ] `batchResultService` のURL手組みをやめ `params` 送信へ統一した
- [ ] User一覧画面の送信キーを新仕様へ置換した
- [ ] Role一覧画面の送信キーを新仕様へ置換した
- [ ] Manual一覧画面の送信キーを新仕様へ置換した
- [ ] BatchResults から `+1/-1` のページ変換ロジックを削除した
- [ ] BatchResults のソート列IDと `sortKey` のマッピングを実装した
- [ ] FEの受信型を `total + pageNumber + pageSize` へ更新した
- [ ] API `400` 発生時のエラー表示が既存どおり動作することを確認した
- [ ] FEテスト（API送信キー、ページング、ソート）を追加した
- [ ] 主要画面の手動回帰（User/Role/Manual/Batch）を完了した

## 3. 最終確認チェックリスト
- [ ] 仕様書（要件定義/詳細設計）と実装のキー名が一致している
- [ ] 旧クエリを使うコードがFE/BEともに残っていない
- [ ] PR説明に「統一キー」「旧キー拒否」「回帰結果」を記載した
- [ ] レビュー指摘の反映が完了している

## 4. 更新履歴
| ver | 更新日 | 更新者 | 内容 |
|-----|--------|--------|------|
| 0.1 | 2026/04/13 | 大路 | 初版作成（FE/BE実装チェックリスト） |

