[project]
name = "jems-python-tools"
version = "0.1.0"
description = "Python utility workspace for docker/python-tools."
requires-python = ">=3.12"
dependencies = []

[dependency-groups]
dev = []

[tool.uv]
package = false
